# Meme Stratum — 引き継ぎ資料

## 概要

インターネットミーム53個を「暖かい壁に画鋲で留めた紙」風に3D表示するThree.jsアート作品。
iMac展示を想定したフルスクリーン体験。時間軸（2000〜2026年）をZ方向にスクロールしてミームを発掘する。

## リポジトリ

- **URL**: `git@github.com:unlimish/meme_stratum.git`
- **ブランチ**: `master`（2026-07-01 push済み）
- **ライセンス**: プライベート

## 現行仕様

### スタック
| 要素 | バージョン |
|------|-----------|
| Three.js | r184 |
| Vite | 8.x |
| 言語 | Vanilla JS (ES Modules) |

### ファイル構成
```
/
├── index.html          ← エントリ（canvas + HUD2層 + タイムライン）
├── package.json        ← three + vite 依存
├── vite.config.js      ← （未作成 = Viteデフォルト動作）
├── src/
│   ├── main.js         ← 全ロジック（637行）
│   ├── memeData.js     ← 54ミームデータ
│   └── style.css       ← 全スタイル（330行）
├── dist/               ← ビルド成果物
└── public/             ← （未使用）
```

### ミームデータ（memeData.js）
```js
{ id, name, bornYear, diedYear, color, imageUrl }
```
- 全54件、URLはimgflip.comの実在画像（HTTP 200確認済み）
- `bornYear`: 流行開始年、`diedYear`: 終了年（2026 = 継続中）
- `color`: ミーム固有のテーマカラー（画鋲・トレイルに使用）

### 3Dシーン設計

#### 空間設計
- Z軸 = 時間（2000年→2026年 = Z:-260→0、K_Z=10）
- X軸 = 10レーンのラウンドロビン配置（レーン幅0.5）
- Y軸 = レーン内で垂直スタッキング（範囲 -2.2〜2.2）
- カメラFOV 60°、壁背景色 `#c4b8a5`

#### パネル（紙のメモ）
- `PlaneGeometry` + `MeshBasicMaterial` (depthWrite: false)
- サイズ: `1.0 + (exp(dur/10)-1) * 0.7` → 1年=1.07、22年=6.62
- アスペクト比 1.35（縦長）
- Zジッター ±0.15、Z回転 ±0.03 rad（傾き）
- テクスチャは512x512 Canvasに紙質感・画鋲・画像・タイムライン・ラベルを描画
- 経年劣化（黄ばみ、染み、角の擦れ）は `ageFactor`（0〜1）で制御

#### トレイル（3D時間軸）
- `TubeGeometry(LineCurve3, 1, radius=0.015, 6)`
- ミーム色、opacity 0.2
- Z=誕生年からZ=死年までを結ぶ
- 終点に `CircleGeometry(0.06)` のドットマーカー（opacity 0.3）

#### パーティクル
- 1200個、色 `#c4b09a`、opacity 0.2
- ゆっくり降下

#### 照明 / 環境
- FogExp2 `#c4b8a5`, density 0.006
- AmbientLight `#fff8f0` 0.7
- DirectionalLight `#ffe8d0` 0.5
- `renderer.setClearColor(0x000000, 0)` → CSS背景が透ける

### 操作 / インタラクション

| 操作 | 内容 |
|------|------|
| マウスホイール | 上下スクロール |
| ドラッグ | 上下ドラッグで移動 |
| 矢印キー | 上下左右で1年単位移動 |
| タッチ | スワイプで移動 |
| シリアル（Web Serial API） | エンコーダー値で移動 |
| **20秒無操作** | **自動スクロール開始（1.5 Z/s）、現在地点でループ** |

全イベントで idle timer がリセットされる。

### HUD / UI
| 要素 | 位置 | 内容 |
|------|------|------|
| 巨大年号 | 画面中央 | Outfit font weight 100, opacity 0.12, z-index 0 |
| 時代ラベル | 年号の下 | 6時代で切替（Primordial〜Algorithmic） |
| ミーム情報 | 下部中央 | ミーム名 + 期間 |
| タイムライン | 下部 | 56px/年、マスクで左右フェード |
| シリアル表示 | 右上 | CONNECTED / OFFLINE |
| オーバーレイ | 全面 | ENTERボタン + シリアル接続ボタン |
| 背景 | canvasの後ろ | 2層クロスフェード（blur 16px, saturate 0.5, opacity 0→0.3） |

### オーディオ
- Web Audio API: triangle 55Hz + sine 27.5Hz → lowpass filter
- 経年で周波数低下、スクロール速度でフィルター変調
- ENTERで開始

### シリアル通信
- Web Serial API（115200 baud）
- 改行区切り数値 → `targetZ -= v * 2.5`
- エンコーダー用（Pico W + CircuitPython HID想定）

## 開発の経緯

### アーキテクチャ変遷
1. **初期**: Gantt風Z行スケジューリング（重なり回避の interval scheduling） → 複雑すぎた
2. **ラウンドロビン採用**: 10レーン単純割り当て、垂直スタッキングで解決
3. **サイズ設計**: 線形 → 指数関数スケーリング（短命ミームの視認性と長命ミームの迫力を両立）

### 画像URL問題
- imgflip API `get_memes` では上位100件のみ取得可能で、53件中7件しかマッチしなかった
- 残り46件は各ミームのmemegeneratorページをスクレイピングして確認
- 全件i.imgflip.com上の実在画像（HTTP 200確認済み）

## ハードウェア計画

### エンコーダー（展示会用）
| 部品 | 品番 | 価格 | 購入元 |
|------|------|------|--------|
| ロータリーエンコーダー | RE160F-40E3-20A-24P-003（秋月 100292） | ¥200 | 秋月電子 |
| メタルノブ | K-29-6.1（秋月 112202） | ¥460 | 秋月電子（ネジ付属） |
| 六角レンチ | 1.5mm（秋月 103185） | ¥250 | 秋月電子 |
| Pico W | — | 既存 | — |
| **合計** | | **¥910** | 秋月で一括購入可 |

#### Pico W配線
```
エンコーダー         Pico W
A相 ───────────── GP2
B相 ───────────── GP3
COM ───────────── GND
SW  ───────────── GP4 (+ プルアップ)
```

#### 秋葉原店舗情報
- エンコーダー: 1F 30番売場 3段目（在庫30個）
- ノブ: 1F 7番売場（在庫11個）
- 通販も可（送料¥500、2日程度）

## 残タスク（優先度順）

### [高] 講評会用
- [ ] Pico Wファームウェア作成（CircuitPython HID、エンコーダ→左右矢印キー）
- [x] 筐体3Dプリント用STL設計済み（hardware/case/ 参照、印刷は未）
- [ ] 動作確認（ノブ＋Pico W＋macOS接続）

### [中] 品質向上
- [ ] 自動スクロール中に視覚インジケーター表示
- [ ] カメラ揺れの微調整
- [ ] パネル配置の詰まり改善（重なりが気になる箇所あり）
- [ ] モバイル対応（レイアウト崩れ確認）

### [低] リファクタリング
- [ ] main.jsをファイル分割（テクスチャ生成、シーン構築、アニメーションループ）
- [ ] `meme.text` の活用
- [ ] Three.js CDN → importmap に統一

## 謝辞

並走してくれてありがとうございました。来週の講評会がうまくいきますように 🚀
